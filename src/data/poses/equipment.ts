import type { PoseSet } from "@/lib/pose-types";

/**
 * Pose sets for the equipment expansion (kettlebell, single-dumbbell and
 * band movements). Populated by the equipment content release.
 */
export const equipmentPoses: PoseSet[] = [];
